type Module @model {
  id: ID!
  title: String!
  description: String!
  link: String!
  pictureLocation: String
  tags: [String]
}

type SudokuPuzzle @model {
  id: ID!
  entries: [Int]!
}

type PathFinderPuzzle @model {
  id: ID!
  entries: [Int]!
}

type ModulePage

type DivEntry {
  type: DivType!
  content: String!
}

enum DivType {
  paragraph
  image
  gif
}